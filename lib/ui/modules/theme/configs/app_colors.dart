import 'package:flutter/material.dart';

class AppColor {
  // set green as primary color
  static const Color primaryColor = Color(0xFF00B800);

  // set black with opacity 0.8 as primary text color
  static Color black = const Color(0xFF000000).withOpacity(0.9);

  static Color white = const Color(0xFFFFFFFF);
  //container
  static Color grey = const Color.fromARGB(255, 203, 203, 203);
  static Color darkGrey = const Color.fromARGB(255, 100, 100, 100);
}
